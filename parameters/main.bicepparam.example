using '../main.bicep'

param apiCenterName = '<your-api-center-name>'

param apiCenterLocation = 'eastus'

param readerGroupPrincipalId = '<entra-security-group-object-id>'

param apimPublisherEmail = '<publisher-email@example.com>'

param apimPublisherName = '<Organization Name>'

param tenantId = '<entra-tenant-id>'

param tags = {
  env: 'dev'
  project: 'mcp-registry'
}

// Optional parameters (uncomment and customize if needed):
// param adminPrincipalIds = ['<service-principal-object-id>']
// param vnetName = '<custom-vnet-name>'          // Default: ${apiCenterName}-vnet
// param apimName = '<custom-apim-name>'          // Default: ${apiCenterName}-apim
// param appInsightsName = '<custom-appi-name>'   // Default: ${apiCenterName}-appi
// param logAnalyticsName = '<custom-law-name>'   // Default: ${apiCenterName}-law
